{
  "name": "@adops/canva-client",
  "version": "0.1.0",
  "private": true,
  "description": "Canva API client scaffold — no OAuth in repo",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "echo 'scaffold — implement in Phase 6'"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
