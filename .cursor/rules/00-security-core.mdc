---
description: Security GIGA core — defense-only, ASSESS-ONLY default, K-003.
alwaysApply: true
---

# Security Core

- In-repo Security GIGA; Claude Code paste **cancelled**. Ref: `docs/CURSOR-SECURITY-GIGA-MASTER-PROMPT.md`.
- Read `SECURITY_STATE.md` before security work. Default `MODE=ASSESS-ONLY`.
- Defense-only: no exploit/PoC/bypass/phishing/C2. Prefer D3FEND; ATT&CK=detect map only.
- Secrets: `${VAR}`, `vault://`, `op://`, `<REDACTED>` only.
- K-003: no 900k blob; no invented top-100 experts.
- Coexist with Canva/agency rules. Owner chat: Turkish terse.
- Stamp `AUDIT_LOG.jsonl` + `BILGI_TABANI.md` after ops.
