---
description: English alias for /devam — resume GIGA agency session from STATE checkpoint.
---
# /resume

Same as `/devam`. See `.cursor/commands/devam.md`.

```bash
cat STATE.md
```

Continue from last checkpoint; confirm `CANVA_MODE` before any Canva MCP call.
